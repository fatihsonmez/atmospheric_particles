name: atmospheric_particles
description: "Enhance your Flutter application's visual appeal with atmospheric particle effects. This package provides a simple, performant way to layer subtle, moving particles onto your widgets, transforming static backgrounds into dynamic, immersive environments."
version: 0.0.3
homepage: https://github.com/fatihsonmez/atmospheric_particles
repository: https://github.com/fatihsonmez/atmospheric_particles

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
