/// Defines the possible shapes for particles.
enum ParticleShape {
  /// Particles are rendered as circles.
  circle,

  /// Particles are rendered as squares.
  square,

  /// Particles are rendered as triangles.
  triangle,

  /// Particles are rendered as ovals.
  oval,

  /// Particles are rendered as rounded rectangles.
  rrect,
}
